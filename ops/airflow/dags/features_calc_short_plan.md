План

  1. Подтвердить итоговый статус запуска
     Проверить в Airflow UI/БД состояние features_calc_short_run для scheduled__2026-02-12T08:30:00+00:00 и сверить с логами воркера/подов (почему лог обрезан
     без SUCCESS/FAILED).
  2. Закрыть численную проблему log10(0)
     Найти место вычисления в features_calc_short.py и добавить защиту (clip/replace(0, nan)), затем нормализацию inf/-inf/NaN перед записью фич.
  3. Ввести контроль качества фич
     Добавить метрики и алерты: число inf/NaN, доля пропусков по символам/таймфреймам, количество срабатываний защиты от нуля.
  4. Убрать риск fork в многопоточном процессе
     Проверить, где в задаче/библиотеках используется fork, перевести запуск на spawn/forkserver или убрать multiprocessing в этом контексте.
  5. Снизить лишнюю работу по инструментам
     Сделать pre-filter по свежести OHLCV до цикла таймфреймов, чтобы уменьшить skipped/no new data и время выполнения.
  6. Разделить диагностические логи
     Добавить итоговый summary в конце таска: обработано символов, коммитов, полностью пропущено, предупреждений, длительность.
  7. Проверить SLA по источнику OHLCV
     Разобрать лаг 1m/5m/15m/30m на старте и согласовать допустимую задержку с апстримом.
  8. Прогон валидации
     Запустить тестовый DAG-run на ограниченном наборе символов, затем полный прогон и сравнить: ошибки, длительность, доля skip, целостность фич.

  Если нужно, могу следующим шагом развернуть это в формат: что делаем / кто ответственный / оценка времени / критерий готовности.
